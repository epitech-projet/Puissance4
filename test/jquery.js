(function($) {   
    $.fn.puissance_4 = function(option) {
    	var players = {
    		joueur_1 :'red',
        	joueur_2 : 'yellow'
    	};

    	var 

        var settings = $.extend({}, players, options);

        return this.each(function(){

        });
    }
}( jQuery ));
$("body").puissance_4();
